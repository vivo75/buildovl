
USE="minimal -acl -bzip2 -caps -installkernel -e2fsprogs -ensurepip -ftp -man -net -python -smartcard -utils -vim-syntax -webdav -xattr"
#INSTALL_MASK="/usr/lib*/debug /usr/lib*/systemd /usr/share/{doc,man,gtk-doc,vim,}" <- gcc problems?
INSTALL_MASK="/usr/lib*/systemd /usr/share/{doc,man,gtk-doc,vim,}"
PKG_INSTALL_MASK="${INSTALL_MASK}"

#RUSTFLAGS="-C code-model=small -C debuginfo=none -C lto=true -C opt-level=z -C strip=symbols -C codegen-units=1 -C panic=abort -C target-feature=+crt-static"
RUSTFLAGS="-C code-model=small -C opt-level=z"

